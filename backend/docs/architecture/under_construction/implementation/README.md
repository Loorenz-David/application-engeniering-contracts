# Implementation Plans

Plans placed here describe HOW to build something — specific tasks, steps,
contracts to load, acceptance criteria, and validation commands.

Each plan should link back to an intention plan that gives the WHY.

Naming convention: `PLAN_<slug>_<YYYYMMDD>.md`
